A restart-queue status is included in this message's structured details (`requestId` and `state`). If the state is `queued` or `draining`, finish the provider call and tool batch already in progress, then checkpoint this session's current assignment in its durable transcript. Do not start another model turn, emit new tools, spawn or wake agents, or claim the whole goal is complete. Report unresolved external effects or missing receipts as explicit blockers; never infer completion when an effect result was not persisted. The controller will restart the process after the current work is safe. If the state is `cancelled`, the user cancelled this request: disregard earlier queued notices for the same request and resume normal work without preparing or issuing a restart.
